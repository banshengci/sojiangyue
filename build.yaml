# build_runner 配置
# lib/design/ 是独立设计模块（不参与应用构建），排除以避免
# flutter_svg 依赖树与 build/source_gen 版本冲突导致的
# "Cannot recurse at later or equal phase 3" 错误。
targets:
  $default:
    sources:
      exclude:
        - lib/design/**
